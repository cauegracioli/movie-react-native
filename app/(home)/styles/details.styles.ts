import { StyleSheet } from "react-native";

export const detailsStyle = StyleSheet.create({
  backdrop: {
    width: "100%",
    height: 200,
    opacity: 0.5,
  },
  poster: {
    height: 250,
    aspectRatio: 3 / 5,
    borderRadius: 10,
    marginTop: -80,
  },
});
